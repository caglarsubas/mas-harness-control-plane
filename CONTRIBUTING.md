# Contributing

Work is accepted only through one packet-scoped branch and pull request. Read the hash-pinned packet, predecessor locks, `AGENTS.md`, and `SECURITY.md` before editing. Run acceptance through the trusted deny-all-outbound launcher. Never include credentials, tenant data, warm-source material, generated secrets, or billable service defaults.
